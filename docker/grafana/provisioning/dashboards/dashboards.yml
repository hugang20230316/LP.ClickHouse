apiVersion: 1

providers:
  - name: 'ClickHouse'
    orgId: 1
    folder: 'ClickHouse'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards # 自动加载项目里预置的仪表盘 JSON。
